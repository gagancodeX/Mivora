const board=document.getElementById("board"),hint=document.getElementById("hint"),restart=document.getElementById("restart");const order=["leaf","sky","sand","light"];function start(){board.innerHTML="";let i=0;hint.textContent="Order: leaf → sky → sand → light";["leaf","light","sky","sand"].sort(()=>Math.random()-.5).forEach(c=>{const b=document.createElement("button");b.className="flow-dot "+c;b.onclick=()=>{if(c===order[i]){b.classList.add("done");i++;hint.textContent=i===4?"Flow complete ✦":"Good. Next: "+order[i]}else{hint.textContent="No rush. Start again.";document.querySelectorAll(".flow-dot").forEach(x=>x.classList.remove("done"));i=0}};board.appendChild(b)})}restart.onclick=start;start();