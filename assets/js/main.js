const body=document.body,theme=document.getElementById("themeBtn");if(localStorage.getItem("mivora-theme")==="light")body.classList.add("light");if(theme)theme.textContent=body.classList.contains("light")?"☀":"☾";theme?.addEventListener("click",()=>{body.classList.toggle("light");localStorage.setItem("mivora-theme",body.classList.contains("light")?"light":"dark");theme.textContent=body.classList.contains("light")?"☀":"☾"});let seconds=0;const session=document.getElementById("session");if(session)setInterval(()=>{seconds++;session.textContent=Math.floor(seconds/60)+" min session"},1000);